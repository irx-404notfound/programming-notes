# JAVASCRIPT SPECIALIZATION

## NO PRIMITIVE DATA TYPES

### ARRAY

> [!NOTE]
> Array's are objects that allows to save a collection of elements in a variable.

- It saves all of diferent types of data.
- Each data have an indice.
- The first data have the indice 0.
- It are redeemable.

Example:

```javascript
  let user = [];
```
